import * as Preference from "./preference/actions";
import * as Layout from "./layout/actions";
import * as Wallet from "./wallet/actions";
import * as Token from "./token/actions";
import * as Swap from "./swap/actions";
import * as Pool from "./pool/actions";

export default { Preference, Layout, Wallet, Swap, Token, Pool };